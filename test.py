from DATABASE.database import DatabaseManagement, Database, UserDatabase, BlogDatabase

"""with DatabaseManagement('user_database.db') as connection:
    database = UserDatabase(connection)
    database.create_table()
    database.add_user(user_data=('yahya_05', 'gnaouiyahya004@gmail.com', '89', 'log in'))

with DatabaseManagement('blog_database.db') as connection:
    database = BlogDatabase(connection)
    database.create_table()
    # database.add_post('Creating digital products that strike a nerve. Thanks to his study of psychology, Ramit identified common financial and career-based pain points that people face as they journey through life.', 'Lebron James')
    database.add_post('The greatest glory in living lies not in never falling, but in rising every time we fall', 'Nelson Mandela', post_id=100 )
    database.add_post("Your time is limited, so don't waste it living someone else's life. Don't be trapped by dogma – which is living with the results of other people's thinking.", 'Steve Jobs')
"""


